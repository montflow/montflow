# montflow

TypeScript monorepo + agent skills.

## Packages

- `@montflow/core` — TypeScript utilities

## Skills

```bash
npx skills add montflow/montflow --list
npx skills add montflow/montflow -a pi
```