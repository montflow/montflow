/**
 * Standard key for "Id" property in custom structs.
 * Used to identify the type of custom struct
 */
export const Id = "_id" as const;

/**
 * Standard key for "Tag" property in custom structs.
 * Used to identify the variant of custom struct
 */
export const Tag = "_tag" as const;
